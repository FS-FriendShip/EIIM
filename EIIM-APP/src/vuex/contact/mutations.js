import * as types from './types.js'

export default {
  [types.UPDATE_CONTACTS] (state, contacts) {
    state.contacts = contacts
  },

  [types.UPDATE_CONTACTS] (state, contacts) {
    state.contacts = contacts
  }
}
